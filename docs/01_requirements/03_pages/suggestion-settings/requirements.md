# 提案とまとめの条件の機能要件

## 目的

提案とまとめの使い方を決める。

## このページでできること

| ID | 機能 | 操作後にできること・確認できる結果 |
|---|---|---|
| suggestion-settings-F01 | 提案のタイミングを選ぶ | 自分で開いたときか継続受取かを選べる。 |
| suggestion-settings-F02 | 集計期間を決める | まとめに使う記録の範囲を選べる。 |
| suggestion-settings-F03 | 停止対象を管理する | 場所と活動ごとの提案停止を確認して解除できる。 |

## 保存と再表示

提案タイミング・まとめの対象期間・場所と活動ごとの停止/解除。

## 成立条件と例外

- 感想の低評価と、特定の場所＋活動に対する明示的な停止を別扱いにする。停止は本人が解除するまで維持し、全体の提案タイミングを守る。
- 対象データがない場合は、その対象の0件状態と次にできる操作を表示する。取得できない状態を0件と同一視しない。
- 保存や取得に失敗した場合は入力と対象を保持し、処理結果が分かる状態で再試行できる。
- [共通要件](../common.json)の画面サイズ・文字拡大・メニュー開閉時の操作可能性を満たす。

## 詳細との対応

[構成要素](components.json) / [操作・遷移](interactions.json) / [API](api.json) / [機能要件JSON](requirements.json) / [受入条件](acceptance.json)。
受入条件は未実行。機能の完了は表示だけでなく、必要な保存・再表示を含めて判断する。
