# 相棒の制作の機能要件

## 目的

希望に合う相棒を制作する。

## このページでできること

| ID | 機能 | 操作後にできること・確認できる結果 |
|---|---|---|
| companion-create-F01 | 希望を書く | 名前20文字・外見200文字と任意の参考画像を入力できる。 |
| companion-create-F02 | 作り方を選ぶ | 接続済みの生成先を使うか下書きや制作指示を持ち出せる。 |
| companion-create-F03 | 候補を確認する | 生成進捗と結果の動きを見て採用できる。 |
| companion-create-F04 | 後で再開する | 未接続や失敗でも入力を残せる。 |

## 保存と再表示

制作下書き・生成処理の状態・生成候補・採用結果。採用と現在の相棒への設定は分ける。

## 成立条件と例外

- 接続先を確認し、入力→生成→動作確認→採用→選択へ進む。未接続時は生成を無効にし、下書き保存と指示の持出しを使える。生成結果を自動採用しない。
- 対象データがない場合は、その対象の0件状態と次にできる操作を表示する。取得できない状態を0件と同一視しない。
- 保存や取得に失敗した場合は入力と対象を保持し、処理結果が分かる状態で再試行できる。
- [共通要件](../common.json)の画面サイズ・文字拡大・メニュー開閉時の操作可能性を満たす。

## 詳細との対応

[構成要素](components.json) / [操作・遷移](interactions.json) / [API](api.json) / [機能要件JSON](requirements.json) / [受入条件](acceptance.json)。
受入条件は未実行。機能の完了は表示だけでなく、必要な保存・再表示を含めて判断する。
