# #189 記録2フローの追加修復（2026-09-25 夜）

## 環境と比較対象

`origin/develop d6f8d91`から専用worktree `visual-records-camera-189` をfast-forwardし、Task `VISUAL-RECORDS-CAMERA` を正規claim（generation 9）。変更は `src/features/records/` とこの証拠フォルダだけ。Chromium/agent-browser 0.18.0、製品Vite `127.0.0.1:5174`、ローカルAPI `127.0.0.1:3191`、liveモードの隔離一時DB。`5173`は他タスクが使用中のため触れていない。元cloneのMapbox設定を持ち込んでおらず、地図タイル/地図上候補の表示は「接続設定がありません」となる。生成写真は `tools/local/demo-media/assets/` のcafe/bookshopで、実機カメラや私物写真ではない。

原本 `docs/01_requirements/03_pages/references/Codex 画像 2026年9月15日 08_07_53.png` の作成・場所選択・確認、および `08_07_57.png` の編集を開いて比較。原本のカフェ写真・地図3候補・気分/滞在時刻は今回の独立実データと異なる。原本幅は場所432×903、編集426×930。既存の `*-reference-fixture.png` は表示fixtureで保存通信の証拠ではない。

| フロー | 修正前の再現 | 今回の修正と実画面 | 残る確認 |
| --- | --- | --- | --- |
| 作成：場所選択→確認→戻る/取消 | 選択済みの場所で「場所を未指定で戻る」を押しても場所が残った。地図/検索/履歴タブ切替や検索語変更後、旧候補・旧選択が残り、別条件でも旧選択の確定ボタンが有効だった。通信断が英語の`Failed to fetch`表示。媒体失敗後は「媒体を処理しています」が残った。 | 明示的な「未指定で戻る」は場所・選択・訪問チェックを解除し、通常の「戻る」は既存の場所を保持。タブ/検索語変更で旧候補・選択を消し、進行中の古い検索を中止。検索欄の空/未送信/0件を区別し、通信失敗を日本語化。媒体失敗時は進行表示を消した。[432px訪問履歴](2026-09-25-place-history-432.png)、[390px検索失敗](2026-09-25-place-search-failure-390.png)、[390px媒体失敗・場所未指定](2026-09-25-create-media-failure-390.png)。 | Mapbox地図上の候補操作、OS拒否/実機撮影/動画、200%文字とソフトキーボードは未確認。 |
| 編集：媒体と下書き→取消/保存/失敗/競合 | 写真順序を変えて「元の一覧に戻る」後に同じ記録を開くと、未保存順序が残った。媒体失敗時に「媒体を処理しています」が残った。 | 明示的な戻りで編集下書きを破棄し、同じIDの保存済み媒体順序に戻る。実画面で写真追加→先頭へ移動→旧写真1枚削除→保存、再取得のposition 0/1を確認。その後、別写真追加時に媒体POSTを遮断し、本文先行保存・失敗写真/入力保持、遮断解除後の再試行を確認。[390px保存後](2026-09-25-edit-media-reload-390.png)、[原本幅426px](2026-09-25-edit-media-reload-426.png)、[PC1440px](2026-09-25-edit-media-reload-1440.png)。 | 実機のファイル選択・OS拒否、動画、リロードをまたぐ未送信File復元、412（409は下記で確認）は未確認。 |

## 実APIのIDと操作順

1. 作成画面で実APIの訪問履歴1件（名古屋駅）を選び、通常の戻りでは未確定選択を捨て、改めて選択・確定後は本文と場所を確認。確認→編集でも本文と場所が残る。「場所を未指定で戻る」後の確認画面では場所未設定・訪問チェック無効。検索語を名古屋駅→東京駅と変えると旧候補と選択が消えた。検索前の案内、存在しない語の0件、候補APIを遮断した通信失敗と再試行後の1件を確認。
2. `recordId=ea136b58-3899-4404-a090-a2f80888e7ac` は本文+生成写真1枚を画面保存。媒体POSTだけをagent-browser network routeで遮断し、本文保存済み/写真失敗を表示。そのrouteを解除して「もう一度試す」を押すと同じrecordIdで媒体`d612f610-be66-4d2a-a639-d5606f12cbda`がready/position 0になった。障害はブラウザ側で制御したもので、API実障害の証拠ではない。
3. 既存の実API `recordId=dcb3af68-7b62-4bcf-b8a7-4d32d8aa82b7` を編集。取消ではAPIの元position 0/1は不変。保存操作で媒体`888532c3-2584-430a-841a-259d52dbffe6`がposition 0、`eda0f2c8-6e7a-47ee-bc3a-2250c6142bdb`がposition 1、元の`06173ab0-ba72-46b8-820c-b23abd7fabe0`が削除された。次の媒体送信遮断後、本文は保存され写真が失敗として残り、再試行で新媒体`ef08bf97-0b32-463f-bfea-da4e29372700`がposition 2/ready。reload後も同一IDで3枚表示。
4. 同じ記録の編集画面で本人下書きを入力したまま、別API PATCHにより版9→10・本文を「並行更新側の本文」へ変更。画面保存は409となり、下書きを保持。[390px競合表示](2026-09-25-edit-conflict-390.png)。画面の「現在の内容を読み直す」で保存済み版10と本人下書きを同時表示し、再保存で本人本文が版11に反映された。別PATCHは隔離DBで競合を再現するための操作で、製品UIからの第二編集ではない。

## 未達の区別

この2フローの空/0件・通信失敗・媒体部分失敗・競合・取消/戻るを実ブラウザで確認した。読み込み中表示はコード上にあるが、短時間の実API応答のため画像で未捕捉。前回の[7画面全体表](2026-09-25-verification.md)と#11の[受入棚卸し](../UI-RECORDS/acceptance-inventory.md)に残る気分・訪問時刻・AI理由・削除preview/export・実成長・実機端末などは今回の完了としない。#11/#284のファイルと古いdirty worktreeは編集していない。

## コード確認

- `mise exec -- bun x vitest run src/features/records/record-flow.test.ts`: 7件成功。
- `mise exec -- bun x vite build`: 成功。大きいchunkの既存警告あり。
- `git diff --check`: 成功。
- `mise exec -- bun run typecheck`: 失敗。診断は `server/core/core.test.ts`、`src/features/exploration/flow.ts`、`src/features/friends/screens.tsx`、`src/features/reflection/DiaryScreen.tsx`、`tools/local/dev.ts` に限られ、今回変更した `src/features/records/` の診断はない。
