# docs圧縮の検証（2026-09-15）

## 結果

対象は `docs/`。誤って編集した `AGENTS.md` は原文へ復元し、その検証記録は削除した。

| 対象 | 変更前 | 変更後 | 削減率 |
|---|---:|---:|---:|
| Markdown | 751,853 bytes | 573,033 bytes | 23.8% |
| JSON | 5,963,917 bytes | 2,737,194 bytes | 54.1% |
| 上記合計 | 6,715,770 bytes | 3,310,227 bytes | 50.7% |
| OpenAPI（JSONの内数） | 3,938,185 bytes | 711,462 bytes | 81.9% |

数値はこの検証記録を除く。画像・スクリプト・SQLは上記集計に含めない。画像33枚は全てバイト一致。元の724ファイルは全て残し、共通HTTP定義と文書索引を追加した。150ファイルを変更、574ファイルはバイト一致。

## 必要・不要の判断

OpenAIは、Astraが長い指示に従える一方、曖昧・競合する文脈に敏感だと説明している。文書量だけを理由に要件を削る根拠はないと判断し、重複を共通定義への参照に置き換えた。[公式説明](https://developers.openai.com/api/docs/guides/latest-model#instruction-following)

テストは変更に見合う範囲とし、成功後の反復には理由を求める方針も確認した。既存の受入条件を一律に不要とは扱わない。[Testing and verification](https://developers.openai.com/api/docs/guides/latest-model#testing-and-verification)

| 扱い | 内容 |
|---|---|
| 保持 | 機能、保存・再表示、数値制約、状態、失敗時、API契約、画面画像と領域、DB・共通基盤、未確定事項、受入シナリオ |
| 共通化 | APIヘッダー・エラー表、同じHTTP応答とパラメータ、67画面の共通成立条件、25画面の閲覧時の保存規則 |
| 再掲を削除 | API操作ごとの型の展開・JSON例、READMEに重複した86の画面要件、同じファイル構成説明。型・例・要件の正本へのリンクを維持 |
| 現状に更新 | API文書3か所の「画面仕様が空」。67画面への参照に更新し、実動作未検証とQ11の未完了は維持 |
| 確認済み判断に統一 | friend-profileの古い「フォローと友達を区別」を、元から記載済みのQ-FRIEND・requirements.jsonの申請／承認制へ揃えた。新しい仕様判断は追加していない |

## 情報保持の検証

- OpenAPIの共通参照を展開し、104操作の全オブジェクトを原本と比較。入力・応答・例・説明・ヘッダー・並び順・未確定依存が一致。144 Schemaとトップレベルの既存メタデータも一致。
- API文書から除いたJSON例がOpenAPIに残っていることを確認。元の表・条件・説明は、新しい機能別文書・型一覧・共通HTTP定義に残っている。型一覧は重複した定義と空行を除き、元の全行を保持。
- 67画面の機能表225行を保持。機能・状態・操作・受入などの画面JSONは全てバイト一致。requirements.mdから除いた文は、共通索引へ移した条件と重複リンクだけ。
- 共通条件3項目の201回分の再掲を1か所へ集約し、67画面から明示的に参照。READMEの86要件は同じ画面のrequirements.mdとの一致を確認して再掲を削除。
- OpenAPI以外のJSONはパース後の全内容が一致。source-manifestはキー順のみ変更し、DB索引を含む元の全項目を保持。
- コンペルール・技術スタック・DB・共通基盤は全ファイルがバイト一致。画像33枚もハッシュ一致。
- ローカルリンク、Markdown見出し、JSON Pointerを2,515件検査し、参照切れなし。文書外の参照は実リポジトリへ解決した。

## 定義検査

| 検査 | 結果 |
|---|---|
| API `check_contracts.mjs` | 104操作・144型・1,002例・不正入力8件：通過 |
| 画面 `verify.mjs` | 67画面・33画像・998部品・553操作・317要件／受入シナリオ：通過 |
| 共通基盤 `verify.mjs` | 67定義・50例・93リンク：通過 |
| API再生成 | OpenAPI・型一覧・共通HTTP文書・機能別文書のバイト一致を確認 |

契約検査は共有HTTP参照を解決するよう対応した。既存の入力拒否・例・型・参照検査は維持。生成器も更新し、再生成しても重複を戻さない。実UI・APIサーバー・DB保存の実行結果を示す検査ではない。

## バックアップ

編集前に全723ファイルを複製してハッシュ照合。その後、並行作業で追加された描画指定とREADME追記も別スナップショットへ取り込み、724ファイルを再照合した。圧縮は作業用コピーで実施した。

原本との比較に合格後、実フォルダーへ反映した727ファイルのハッシュ一致を確認。編集前の全バックアップと作業用コピーを削除済み。

- 編集直前manifestのSHA-256：`63677509de7353fe861f76feca5a02b19366d17274110b53ff39cd5020bfb3d6`
- 原OpenAPIのSHA-256：`eccda968ba20a333375f14d527e29a0267bffbfa3c3e450f178730ad5c7e8c58`
- 圧縮OpenAPIのSHA-256：`0d7e33d33a58cac42e4498032aa6b12e71c311b841eac35cf1336f5ee212d322`
