## 変更

#35の相棒登録・現在選択・制作下書き・生成状態を本人別SQLiteへ別保存します。全必要動作を確認するまで登録/採用せず、生成成功で自動採用・自動選択しません。下書きの入力版と生成時の入力を分離し、取消後の遅着結果を破棄します。参考画像の本人別保存と指示持出し、固有API断片v0.1.0も含みます。

## 検証

- 実SQLiteで保存/再オープン、版競合、本人分離、登録/採用/選択分離を確認。
- 保存層4件、参考画像1件、生成サービス境界2件の計7テスト成功。TypeScript型検査成功。
- 取込・provider fixtureは保存/制御境界専用。実ZIP互換や外部生成の証拠ではありません。

## 未完了

CORE.runtime/integrationの登録・共通HTTP/再送ラッパ、正式v2互換仕様/fixture・展開上限、画像decode/ZIP依存、実生成先/資格情報が未接続です。実HTTPの保存/再取得とUI #19接続を続けます。このPRはdraftで、COMPANION.import/create完了・Issue close・board finishを要求しません。

Refs #35
