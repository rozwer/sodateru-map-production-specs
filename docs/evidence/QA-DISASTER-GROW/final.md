# 最終確認 — QA-DISASTER-GROW #217

## 記録の主要保存

- 配信HEAD `04bdbceec67f76dabee5b945a1e317f8c6e5e4fa` を開始・終了で確認。
- 既存demo本人、390×844、既存coffee.jpg、本文「QA-DISASTER-GROW 最終受入：写真付き記録の保存と再表示。」、場所/日時未指定、公開範囲「自分だけ」で保存。
- `recordId=ff147c28-0796-4766-b59c-2045aac1b48b` の今日の軌跡へ遷移。reload後、同本文・写真1を再表示。
- `.local/demo.sqlite` をread-onlyで確認。同ID、version=2、本文一致、visibility=private。記録は削除せず残した。live領域には投稿しない。
- 画像 `final-record-saved-390.png`。保存前の両幅入力は `record-entry.md` のPR240節。

## 境界

写真付き記録の保存・再表示は確認済み。場所/訪問の確認から建物着色、全record用途訂正、live領域の保存は本検証に含めない。

## 防災・GROWの最後の操作確認

2026-09-15 13:52–13:57 JST、運用担当が固定した配信HEAD `c38a0fb7b62e8b1d1db2efecac5dd7c9e9298ca9`。共有checkout `/Users/roz/.codex/worktrees/qa-visual-40`、UI `http://127.0.0.1:5173`、API `3002`、既存demo本人、390×844。DB・session・portを変更せず操作した。13:57以降は運用担当へ利用終了を連絡し、後続設定修復の切替を許可したため、このHEAD以降の動作は本証拠に含まない。

| 操作 | 観測した結果 |
| --- | --- |
| ストア初期表示 | 防災→バイク→聖地、v0.1、未導入を表示。バイク・聖地・お願いの模擬操作（未保存）と、防災の保存を区別している。 |
| 防災詳細→試してみる | `#/disaster-map?pluginId=fixture-disaster&intent=trial` の専用画面へ遷移。バイク用条件は表示されず、模擬であることを表示。 |
| 試用条件→導入確認→導入 | 降水をOFF、地域を名古屋・本山に変更。導入確認に地域と洪水想定・地形が引き継がれ、導入成功後に実情報更新の操作を表示。 |
| 防災情報を更新 | 洪水・地形が取得済み13:53、OFFにした降水は未取得。凡例に国土地理院の取得元、取得日時2026/9/15 13:53:24と配信元の更新日時を分けて表示。 |
| 専用画面を再読込 | 名古屋・本山、洪水ON・地形ON・降水OFF、取得日時を保持。地図canvasは1つ、横幅・scrollWidthとも390。実地図上の灰色の矩形レイヤーを目視。 |
| 街の地図へ→管理画面 | 通常地図 `#/map` に戻る。管理画面では防災ON・使用中を表示。 |
| 管理画面で防災OFF→再読込 | OFF・停止中を保持し、「地図で使う」が無効。停止した設定を残して終了。 |

画像: `final-grow-store-390.png`、`final-disaster-trial-390.png`、`final-disaster-updated-390.png`、`final-disaster-reload-390.png`、`final-disaster-stopped-390.png`。

## 残件と引継ぎ

- 防災の専用入口、地域・レイヤー設定、取得元・時点表示、再読込保持、管理OFFの主要経路は上記の範囲で確認済み。最後の防災確認ではDBを直接照合していないため、保存証拠はブラウザ再読込まで。
- リハーサルの防災表現との全面一致は未達・未確認。3D地形・流域表現は今回の目視で成立を確認できず、矩形レイヤー表示を同等品質と扱わない。避難所・開設状況・警報・津波・流域等の不足は画面の説明にも残る。
- バイク・聖地・お願いは引き続き模擬操作で、保存済み機能の完成には数えない。最後のバッチでは防災以外の固有試用・全状態・全原本一致を再検証していない。
- 記録の主要保存は上記の別HEADで確認済み。最後の防災HEADでの再保存、全用途、場所確定から建物着色は未検証。
- Task/Issue #217 は全面受入完了とせず、証拠PR統合後にclaimを返却しIssueをopenで残す。後続はこのファイルと `baseline.md`、`grow-226.md`、`record-entry.md` を起点に不足項目だけを検証する。

この作業は実装を変更せず、証拠だけを追加した。共有ランタイムと既存データを保持したまま独立QAを終了する。
